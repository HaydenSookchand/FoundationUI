import colors from './colors';

const borders = {
  default: `1px solid ${colors.outlinedBorder}`,
  hover: `1px solid ${colors.outlinedHoverBorder}`,
  rounded: '4px',
  circle: '50%',
};

export default borders;
