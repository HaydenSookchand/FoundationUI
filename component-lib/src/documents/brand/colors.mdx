import { Meta } from "@storybook/blocks";
import { Card, CardContent, Typography, Button } from "@mui/material";
import colors from "../../theme/colors";

<Meta title="Brand/Colors" />

# Color Palette
<div className="color-container" style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(150px, 1fr))', gap: '16px', marginTop: '20px' }}>
  {Object.entries(colors).map(([name, color]) => (
    <Card key={name} style={{ width: '120px', backgroundColor: 'white', borderRadius: '8px', overflow: 'hidden', boxShadow: '0px 2px 5px rgba(0,0,0,0.1)' }}>
      <div style={{ backgroundColor: color, width: '100%', height: '50px' }} />
      <CardContent style={{ textAlign: 'center' }}>
        <Typography variant="body1" style={{ fontWeight: 'bold' }}>{name}</Typography>
        <Typography variant="caption" style={{ color: '#666' }}>{color}</Typography>
      </CardContent>
    </Card>
  ))}
</div>
